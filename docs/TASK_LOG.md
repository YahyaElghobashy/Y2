# Y2 Task Log

> Auto-updated by the orchestrator after each completed task.

| Task ID | Name | Status | Completed At | Commit | Notes |
|---|---|---|---|---|---|
| — | Project documentation setup | ✅ | (manual) | (initial) | CLAUDE.md + all docs created |
| T101 | BottomNav | ✅ | — | 0a16ea7 | Fixed bottom nav with 5 tabs, copper slide indicator, Framer Motion |
| T102 | PageHeader | ✅ | — | — | Page identity header with back nav, right action slot, balanced layout |
| T103 | PageTransition + FadeIn + StaggerList | ✅ | — | — | Motion language: 3 animation wrappers (page entrance, fade, stagger list) + barrel export. Respects prefers-reduced-motion. 14 tests passing. |
| T104 | LoadingSkeleton | ✅ | — | — | 4 variants (card, list-item, header, full-page). CSS-only animate-pulse, warm bg-secondary tones, Server Component. 8 tests passing. |
| T105 | EmptyState | ✅ | — | — | Reusable empty state shell with icon, title, subtitle, optional copper CTA (Link or button). FadeIn entrance animation. 9 tests passing. |
| T106 | QuickActionCard | ✅ | — | — | Module doorway card for home 2×2 grid. Icon circle + label + description. Framer Motion press/hover. Wrapped in next/link. 7 tests passing. |
| T107 | AppShell | ✅ | — | — | Root layout shell. Warm cream bg, dvh viewport height, pb-24 content padding, renders BottomNav. Integrated into layout.tsx. 5 tests passing. |
| T108 | Home Dashboard | ✅ | — | — | Home page: HomeGreeting (time-aware greeting + date), 2×2 QuickActionCard grid (Us/Health/Spirit/Ops), 2 WidgetSlots for future widgets. Wrapped in PageTransition. 16 tests passing (9 HomeGreeting + 7 page). |
| T110 | Health Module Shell | ✅ | — | — | /health page shell. PageTransition + PageHeader + EmptyState with Activity icon. Warm copy "Your wellness, tracked". No interactivity, no data fetching. 6 tests passing. |
| T111 | Spirit Module Shell | ✅ | — | — | /spirit page shell. PageTransition + PageHeader + EmptyState with Sun icon. Contemplative copy "Your daily practice". No interactivity, no data fetching. 6 tests passing. |
| T112 | Ops Module Shell | ✅ | — | — | /ops page shell. PageTransition + PageHeader + EmptyState with CheckSquare icon. Practical copy "Life, organized". No interactivity, no data fetching. 6 tests passing. |
| T113 | Settings Page | ✅ | — | — | /settings page. SettingsRow reusable component + settings page with profile card (hardcoded), 3 sections (Account, Appearance, About), Log Out stub, "Made with love for Yara" easter egg. 13 tests passing (6 SettingsRow + 7 page). |
| T201 | Auth DB Migration | ✅ | — | — | Supabase migration: profiles table (8 columns), set_updated_at trigger, handle_new_user auto-profile trigger, 3 RLS policies (own read, partner read, own update), partner_id index, placeholder seed for Yahya+Yara pair. Verification SQL in supabase/tests/. |
| T301 | CoYYns DB Migration | ✅ | — | — | Supabase migration: coyyns_wallets table (7 columns, CHECK balance >= 0), coyyns_transactions table (8 columns, immutable ledger), handle_coyyn_transaction trigger (validates type/amount sign, updates wallet balance + lifetime counters, security definer), reuses set_updated_at for wallets. 4 RLS policies (wallet own read, wallet partner read, transaction own read, transaction own insert). No client UPDATE/DELETE on either table. Compound index on (user_id, created_at desc). Seed wallets for both users. Verification SQL in supabase/tests/. |
| T401 | Push Notifications DB Migration | ✅ | — | — | Supabase migration: push_subscriptions table (6 columns, UNIQUE user_id+subscription, jsonb subscription object), notifications table (10 columns, sender/recipient FKs, type/status CHECK constraints, immutable log), daily_send_limits table (6 columns, UNIQUE user_id+date, CHECK >= 0 on all counters). get_or_create_daily_limit() function (security definer, idempotent, race-safe with ON CONFLICT DO NOTHING). Reuses set_updated_at for push_subscriptions. 9 RLS policies (4 push_subscriptions, 3 notifications, 2 daily_send_limits). No client UPDATE on notifications, no client INSERT on daily_send_limits. Verification SQL in supabase/tests/. |
| T403 | Push Notification Edge Function | ✅ | — | — | Supabase Edge Function `send-notification/index.ts` (Deno runtime). JWT auth + sender verification, fetches notification row + recipient push subscriptions, sends Web Push via `npm:web-push` with VAPID signing. Handles CORS preflight, 410 subscription cleanup, idempotent delivery (skips already-delivered), body truncation (200 chars). Uses `Promise.allSettled()` for parallel multi-device delivery. Updates notification status to 'delivered' (partial success) or 'failed' (all fail). Added `supabase` to tsconfig.json exclude. 11 Deno tests covering auth, validation, 410 cleanup, partial/full failure, idempotency. |
| T501 | Period Tracker DB Migration | ✅ | — | — | Supabase migration: cycle_config table (9 columns, UNIQUE owner_id, pill cycle configuration), cycle_logs table (7 columns, UNIQUE owner_id+date, mood CHECK constraint, symptoms text array). Reuses set_updated_at trigger for cycle_config. STRICT OWNER-ONLY RLS — no partner access on either table (the one exception in the entire app). 3 policies on cycle_config (select/insert/update, no delete), 4 policies on cycle_logs (select/insert/update/delete). Index on cycle_logs(owner_id, date DESC). Verification SQL in supabase/tests/. |
| T601 | Love Coupons DB Migration | ✅ | — | — | Supabase migration: coupons table (18 columns, full lifecycle tracking), coupon_history table (6 columns, immutable audit log). 4 CHECK constraints (status, category, no_self_gift, reveal_requires_surprise). handle_coupon_created() trigger (security definer, auto-logs 'created' to history). Reuses set_updated_at for coupons. Surprise coupon RLS: recipient cannot see coupons where is_surprise=true AND surprise_revealed=false — enforced at data layer. 5 RLS policies on coupons (creator read, recipient read non-surprise, creator insert, creator update, recipient update redeem), 2 RLS policies on coupon_history (creator/recipient read, actor insert). No DELETE or UPDATE policies on either table — coupons are permanent records, history is append-only. 4 indexes on coupons (creator_id, recipient_id, status, partial expires_at), 2 indexes on coupon_history (coupon_id, actor_id). Verification SQL in supabase/tests/. |
| T109 | /us Relationship Module Shell | ✅ | — | — | /us page shell with 4-tab RelationshipTabs (Notes, Coupons, CoYYns, Send). Framer Motion layoutId sliding copper underline, AnimatePresence crossfade content. Each tab shows EmptyState with unique copy. Notes & Coupons have no-op CTA buttons. PageTransition + PageHeader ("Us", back to /). 10 tests passing. |
| T801 | Visual Audit Infrastructure | ✅ | — | — | Screenshot infrastructure for automated visual auditing. `scripts/screenshot.mjs` (Puppeteer headless, 375×812 viewport, networkidle0, full-page PNG) + `scripts/visual-audit.sh` (build → start server on :3099 → screenshot → cleanup). No puppeteer in package.json — uses npx cache. `npm run screenshot` / `npm run screenshot:route` scripts. 7 tests passing. |
| T802 | Visual Audit Orchestrator Integration | ✅ | — | — | Integrated visual screenshot checks into orchestrator auditor. DEFAULTS updated (auditor_max_turns→30, max_audit_minutes→35). AUDIT_JSON_SCHEMA gains `visual_pass` required boolean. Audit prompt gains STEP 6 (visual check via screenshot + Read tool). Audit note header includes `visual:yes/NO`. Screenshot cleanup after every task audit. CONFIG_FLOORS max_audit_minutes raised to 5. Advisory only — visual failures don't block approval. 7 tests passing. |
| T202 | Supabase Client Setup | ✅ | 2026-03-02 | — | Supabase client infrastructure: `database.types.ts` (typed placeholder matching profiles schema from T201), `client.ts` (browser singleton via createBrowserClient), `server.ts` (per-request server client with async cookies()), `middleware.ts` (updateSession for token refresh). All use @supabase/ssr. 4 tests passing (2 client singleton + 2 middleware getUser). |
| T204 | AuthProvider Context | ✅ | 2026-03-02 | 4fc5bd3 | AuthProvider with user/profile/partner state from Supabase onAuthStateChange. useAuth hook with context validation. signOut with /login redirect. Mounted-safe async fetches. profileNeedsSetup boolean + refreshProfile method. 11 tests passing. |
| T203 | Login Page | ✅ | 2026-03-02 | b28c9bf | Login page at /login with RHF+Zod validation, Supabase signInWithPassword, Framer Motion entrance animation, error states, loading spinner. (auth) route group with minimal layout. 13 tests passing. |
| T205 | Auth Middleware + Route Restructure | ✅ | 2026-03-02 | 7379dfd | Moved all pages into (main) route group with AppShell layout. Next.js middleware: unauthenticated→/login redirect, authenticated /login→/ redirect, fail-open on errors. updateSession returns user info. Uses (main) instead of (app) to work around Turbopack emoji byte boundary bug. 11 middleware tests. |
| T206 | Profile Setup Overlay | ✅ | 2026-03-02 | 2cfe7c3 | ProfileSetupOverlay: RHF+Zod name validation, avatar upload to Supabase Storage, 5MB limit check, Framer Motion entry/exit animations. Shows when display_name is empty/User. Integrated into (main) layout. Database types updated with Relationships. 13 tests passing. |
| T207 | Settings Auth Integration | ✅ | 2026-03-02 | c090296 | Settings page uses useAuth() for real profile data. AlertDialog confirmation on sign out. ProfileEditForm with inline expand, avatar upload, initials fallback. Loading skeleton when profile is null. Installs shadcn alert-dialog. 8 + 7 tests passing. |
| T901 | Executor Column + Orchestrator Split | ✅ | 2026-03-02 | — | Added "Executor" column (O) to Task Queue schema. `sheets_connector.py`: `get_eligible_tasks()` accepts `executor` filter param ("orchestrator" or "claude-code"), defaults to no filter. `orchestrator.py`: main loop filters to `executor="orchestrator"`, single-task mode warns if task is claude-code. CLAUDE.md updated with Google Sheets access instructions, column layout, and usage examples. |
| T902 | Orchestrator Retry Logic | ✅ | 2026-03-02 | — | Added `max_retries_per_task` config (default: 1). Failed tasks retry with surgical revert + breather between attempts. `_should_retry()` checks attempt count, logs `TASK_RETRY` event. `_mark_failed()` includes attempt count in sheet output. Retry loop wraps builder + build-verify phases. Config printed at startup. |
| T903 | Claude Code Task Runner CLI | ✅ | 2026-03-02 | — | `orchestrator/cc_tasks.py` — lightweight CLI for claude-code task management. `--start T902` marks building, `--done T902 --commit abc --duration 12m` marks complete, `--fail T902 --reason "..."` marks failed, `--all` shows all tasks with executor column, default shows eligible claude-code tasks. Imports `TaskQueueClient`, logs `MANUAL_START/COMPLETE/FAIL` events. |
| T302 | useCoyyns Hook | ✅ | 2026-03-02 | — | Client-side data layer for CoYYns feature. `useCoyyns()` hook in `lib/hooks/use-coyyns.ts`. Types in `lib/types/coyyns.types.ts`. Database types updated with coyyns_wallets + coyyns_transactions tables. Three parallel initial fetches (user wallet, partner wallet, last 50 transactions). Realtime subscription on coyyns_wallets for live sync. `addCoyyns()` (earn with positive int validation), `spendCoyyns()` (spend with balance check), `refreshWallet()`. Error state management, auth-safe (no-ops when user is null). 13 tests passing. |
| T305 | AddCoyynsForm | ✅ | 2026-03-03 | — | Bottom sheet form for earning CoYYns. Portal to body, AnimatePresence slide-up sheet + backdrop. RHF+Zod validation: amount (1–10,000 int), description (1–200 chars with live count). Calls `useCoyyns().addCoyyns()`, sonner toast on success, root error on failure. X button + backdrop dismiss. Body scroll lock. 10 tests passing. |
| T306 | SpendCoyynsForm | ✅ | 2026-03-03 | — | Bottom sheet form for spending CoYYns. Same sheet pattern as T305. Balance-aware: shows balance, dynamic Zod max(balance), real-time "Insufficient CoYYns" warning with disabled button. Prefilled values support (amount, description, category). Category defaults to "manual". Calls `useCoyyns().spendCoyyns()`. 11 tests passing. |
| T402 | useNotifications Hook + Push Service | ✅ | 2026-03-03 | b1fa7fd | Push service: isPushSupported, getPushPermission, subscribeToPush, unsubscribeFromPush (Web Push API + Supabase push_subscriptions upsert). useNotifications hook: notification list, daily send limits, canSend/remainingSends derived state, sendNotification with optimistic updates + double-tap guard + partner/limit checks. FREE_SENDS_PER_DAY=2. Types in notification.types.ts. 17 tests passing (7 push-service + 10 hook). |
| T502 | useCycle Hook | ✅ | 2026-03-03 | 3f38340 | Cycle tracker data hook: fetches cycle_config + cycle_logs from Supabase, derives currentDay, phase (active/break), daysUntilBreak/Active, isPMSWindow, isPeriodLikely, nextPeriodDate from pill_start_date. Dual-layer privacy: RLS + hook-level owner_id guard. Types in health.types.ts. DB types for cycle_config + cycle_logs tables. updateConfig, addLog, refreshCycle actions. 14 tests passing. |
| T602 | useCoupons Hook | ✅ | 2026-03-03 | 1eef66e | Love coupons data hook: fetches myCoupons/receivedCoupons, derives pendingApprovals via useMemo. Actions: createCoupon, redeemCoupon, approveCoupon, rejectCoupon, revealSurprise with status guards. Realtime subscription on coupons table for live updates. coupon_history audit log for reveals. Types in relationship.types.ts. DB types for coupons + coupon_history tables. 8 tests passing. |
| T308 | CoyynsWidget + Home Dashboard Wiring | ✅ | 2026-03-03 | 276455f | New CoyynsWidget component: compact CoYYns card with CoyynsBadge balance pill, 3 most recent transactions (earn/spend with +/- prefix, comma-formatted amounts), "See all" link to /us, empty state, loading skeleton. HomeGreeting updated to use useAuth() for dynamic display_name (fallback "there"). First WidgetSlot replaced with CoyynsWidget in page.tsx. 12 + 9 + 6 tests. |
| T114 | Hayah Ambient Gradient | ✅ | 2026-03-03 | — | Ambient flowing gradient background. Pure CSS @keyframes (25s ease-in-out infinite, GPU-composited). 3 radial-gradient blobs at low opacity (accent-soft 35%, bg-secondary 50%, accent-primary 6%). HayahGradient component in animations/. Integrated into AppShell + AuthLayout. z-index: 0 fixed, content z-10, BottomNav z-50. Respects prefers-reduced-motion. Zero JS runtime cost. |
| T404 | PushPermissionPrompt | ✅ | 2026-03-04 | bc4e5ee | Push notification opt-in modal. 3-second delayed appearance, Enable/Maybe Later buttons, sessionStorage dismiss tracking, blocked-state fallback, portal + AnimatePresence. 6 tests passing. |
| T405 | NotificationBuilder | ✅ | 2026-03-04 | df8f66e | Notification composition form with emoji picker, title/body fields (RHF+Zod), live preview card, integrated SendLimitIndicator. Calls useNotifications().sendNotification(). 7 tests passing. |
| T406 | SendLimitIndicator | ✅ | 2026-03-04 | 026a3ef | Daily send limit visual indicator. Colored dots (success→warning→error), text count, "Buy more" link when exhausted. Pure presentational. 7 tests passing. |
| T503 | CycleDayWidget | ✅ | 2026-03-04 | df8f66e | Cycle day + phase display with SVG progress arc. Day number, phase (active/break), days remaining, PMS amber glow. Uses useCycle() hook, only renders with config. 7 tests passing. |
| T504 | CycleConfigForm | ✅ | 2026-03-04 | df8f66e | Bottom sheet pill cycle config form. Start date, active/break days, PMS warning days. RHF+Zod validation, portal + AnimatePresence. Upserts via useCycle(). 8 tests passing. |
| T505 | CycleCalendarView | ✅ | 2026-03-04 | df8f66e | Month calendar with phase-colored days (copper=active, rose=break, amber=PMS). Today ring, past dimmed, month navigation. Phase projection from pillStartDate config. 9 tests passing. |
| T506 | CycleInsightCard | ✅ | 2026-03-04 | df8f66e | Contextual advice card for cycle phase. 7 message variants, phase icons (sun/cloud/moon). Warm care-advice tone. Yahya-only (renders when cycle_config exists). 6 tests passing. |
| T603 | CouponCard | ✅ | 2026-03-04 | df8f66e | Gift-style coupon card. Category color badge, status dot, creator label, surprise-hidden state, compact mode. Pending copper glow pulse. FadeIn on mount. 11 tests passing. |
| T407 | BuyExtraPingModal | ✅ | 2026-03-04 | — | Bottom sheet modal for buying extra pings (10 CoYYns). Added `purchaseBonusSend()` to useNotifications hook (upsert daily_send_limits). Uses useCoyyns().spendCoyyns + purchaseBonusSend. Portal + AnimatePresence. 8 tests passing. |
| T408 | PingHistory | ✅ | 2026-03-04 | — | Chat-bubble notification history feed. Sent right-aligned (accent-soft), received left-aligned (bg-elevated). Status icons, date grouping (Today/Yesterday/date). Uses useNotifications() internally. 8 tests passing. |
| T409 | Wire Ping Tab | ✅ | 2026-03-04 | — | PingTabContent wrapper assembling all Ping sub-components. CustomPingComposer (free-text input, 200 char max, enter-to-send). RelationshipTabs updated: renamed "send" to "ping", renders PingTabContent. NotificationBuilder gains onBuyMore prop. 8 tests (CustomPingComposer). |
| T507 | Wire Cycle Tracker | ✅ | 2026-03-04 | — | /me/body page: CycleDayWidget hero, CycleInsightCard, expandable CycleCalendarView, CycleConfigForm modal via gear icon. Setup CTA EmptyState when no config. Fitness placeholder always visible. CycleInsightCard gains compact prop. |
| T508 | HomeCycleWidget | ✅ | 2026-03-04 | — | Home dashboard cycle widget. Thin wrapper: useCycle() → null if no config. "Cycle Tracker" header + Day X phase label + CycleInsightCard compact. Link to /me/body. Replaces WidgetSlot on home page. 6 tests passing. |
| T608 | CouponHistory | ✅ | 2026-03-04 | — | Merged coupon history component. Dedupes myCoupons+receivedCoupons, filters terminal statuses, sorts by activity date desc, groups by month. IntersectionObserver infinite scroll (20/page). Compact CouponCards with activity labels. 10 tests passing. |
| T607 | RedeemConfirmModal + RedeemStampAnimation | ✅ | 2026-03-04 | — | Bottom sheet redeem/approve/deny modal (3 modes). Portal + AnimatePresence. Sonner toasts. RedeemStampAnimation with drop+scale pulse. Respects prefers-reduced-motion. 15 tests passing. |
| T604 | CouponWallet Page | ✅ | 2026-03-04 | — | /us/coupons wallet page. 3 pill tabs (For Me/I Made/History). Pending approvals highlight. Pull-to-refresh. Loading skeleton. CouponHistory in History tab. 10 tests passing. |
| T606 | CouponDetail View | ✅ | 2026-03-04 | — | /us/coupons/[id] detail page. Photo/emoji/title/description, category+status badges, expiry countdown. Role-based actions (redeem/approve/deny). REDEEMED stamp overlay. RedeemConfirmModal integration. 12 tests passing. |
| T605 | Create Coupon 4-Step Flow | ✅ | 2026-03-04 | — | /create-coupon 4-step wizard. Step 1: emoji+title+description+category. Step 2: expiry+surprise toggles. Step 3: photo upload with OffscreenCanvas resize. Step 4: preview+send with paper-airplane animation. Storage migration 009. 8 tests passing. |
| T609 | Wire Coupons Into /us + Home | ✅ | 2026-03-04 | — | HomeCouponInbox (stacked cards widget, count badge, pending glow) + FeelingGenerousCTA (speech-bubble CTA). RelationshipTabs Coupons tab → router.push("/us/coupons"). Both added to home page. 9 tests passing. |
| T702 | MarketplaceItemCard | ✅ | 2026-03-04 | — | Pure presentational marketplace item card. Three states: affordable, unaffordable (opacity-70), coming soon (opacity-60). Price pill with coin emoji + JetBrains Mono. Spring animation. 12 tests passing. |
| T703 | PurchaseConfirmModal | ✅ | 2026-03-04 | — | Purchase confirmation modal. Balance breakdown (Cost/Balance/After). Uses useCoyyns().spendCoyyns(). Disabled when unaffordable. Portal + AnimatePresence. Sonner toast. 15 tests passing. |
| T704 | Wire Extra Notification to Marketplace | ✅ | 2026-03-04 | — | Marketplace page wired with MarketplaceItemCard, BuyExtraPingModal for extra notifications, PurchaseConfirmModal for other items. CreateChallengeForm in Challenges tab. |
| T706 | CreateChallengeForm | ✅ | 2026-03-04 | — | Bottom sheet challenge creation form. Emoji quick-pick (10), title/description/stakes/deadline fields. RHF+Zod validation. Supabase insert to challenges table. Challenges migration (009). 16 tests passing. |
| T707 | ChallengeResolution | ✅ | 2026-03-04 | — | ClaimWinDialog (claim + waiting state + cancel + realtime) + ConfirmResultDialog (confirm/dispute + partial transfer + balance breakdown). Portal + AnimatePresence. 19 tests passing. |
| TF10 | V2: BottomNav 5 Tabs | ✅ | 2026-03-04 | — | BottomNav updated to 5 V2 tabs: Home(House), Us(Heart), 2026(Sparkles, center elevated), Me(User), More(MoreHorizontal). Center tab: -translate-y-1.5, 28px icon, always-copper accent. Home page QuickActionCard grid updated. /2026 route shell created. |
| T115 | V2: /us HorizontalTabBar | ✅ | 2026-03-04 | — | New HorizontalTabBar reusable component (route-based, copper underline, layoutId, sticky, auto-scroll). /us layout with PageHeader + tabs. Sub-routes: /us/coyyns, /us/coupons, /us/calendar, /us/ping. /us redirects to /us/coyyns. |
| T116 | V2: /me Body/Soul Page | ✅ | 2026-03-04 | — | /me landing with Body and Soul section cards. /me/soul EmptyState shell. /me/body backHref updated to /me. /health and /spirit redirect to /me. |
| T117 | V2: /more Utility Drawer | ✅ | 2026-03-04 | — | /more page migrated from settings: profile card, Account, Preferences, About Hayah, Version, Logout with AlertDialog. /more/about page. /ops and /settings redirect to /more. |
| T708 | Marketplace Navigation Wiring | ✅ | 2026-03-04 | — | "Marketplace →" link in RelationshipTabs CoYYns tab. "Shop with CoYYns" button in CoyynsWidget (useRouter to avoid nested <a>). |
| TF01 | Partner Pairing Migration | ✅ | 2026-03-04 | — | Supabase migration 010. Adds invite_code, pairing_status, paired_at to profiles. generate_invite_code() + set_invite_code trigger. pair_partners() and unpair_partners() SECURITY DEFINER functions. Collision-safe code generation. Backfills existing profiles. Partial index on invite_code. |
| T309 | Marketplace Migration | ✅ | 2026-03-04 | — | Supabase migration 011. marketplace_items table (price CHECK >0, effect_type 5-way CHECK, effect_config JSONB, soft-delete is_active). purchases table (buyer/target/item FKs, no-self-purchase CHECK, 5-state lifecycle). RLS: authenticated browse items; buyer/target CRUD on purchases. 5 seed items (Extra Notification 10🪙, Movie Night Veto 25🪙, Breakfast in Bed 40🪙, 1 Hour of Silence 15🪙, Wildcard Favor 50🪙). |
| T314 | Daily Login Bonus | ✅ | 2026-03-04 | — | useDailyBonus hook: checks coyyns_transactions for today's daily_bonus category, inserts +5 earn transaction if none, hasChecked ref for idempotency, UTC date strings, silent error handling. DailyBonusToast component: Framer Motion slide-down toast with coin pulse animation, auto-dismiss 3s, safe-area-inset-top. 12 tests passing (6 hook + 6 component). |
| T701 | Shared Calendar Migration | ✅ | 2026-03-04 | — | Supabase migration 013. events table (14 columns: title, description, event_date, event_time, end_time, recurrence 4-way CHECK, category 4-way CHECK, color override, google_calendar_event_id, is_shared). RLS: read own + shared events, creator-only insert/update/delete. Reuses set_updated_at trigger. Indexes on event_date and creator_id. Realtime enabled. Seed data: Anniversary placeholder. |
| T118 | Home Dashboard Mood Strip | ✅ | 2026-03-04 | — | MoodStrip component: horizontal strip of 4 indicator chips (CoYYns balance, active coupons count, calendar placeholder em dash, pings remaining /2). Uses useCoyyns, useCoupons, useNotifications. Loading: 4 skeleton pills. Graceful degradation: 0 or em dash when data unavailable. Integrated into home page after HomeGreeting. Home page test updated with new hook mocks + V2 QuickActionCard assertions. 15 tests passing (MoodStrip) + 5 tests passing (page). |
| T310 | MarketplaceItemCard Rewrite | ✅ | 2026-03-04 | — | MarketplaceItemCard rewrite with dual variants (horizontal for scroll rows, vertical for grids). Props simplified to {item, balance, onBuy, variant, className}. Exports MarketplaceItemCardSkeleton for loading states. Spring buy animation. 14 tests passing. |
| T311 | HomeMarketplaceRow | ✅ | 2026-03-04 | — | Horizontal scroll row of marketplace items on home dashboard. Uses useMarketplace() + useCoyyns() for data. Renders horizontal MarketplaceItemCard variants. PurchaseConfirmModal integration for inline purchasing. "Shop" link to /us/marketplace. 7 tests passing. |
| T312 | PurchaseConfirmModal Rewrite | ✅ | 2026-03-04 | — | PurchaseConfirmModal rewrite as bottom sheet pattern. Input field for requires_input items (e.g. custom theme name). Purchase records created via useMarketplace().createPurchase. Balance breakdown (Cost/Balance/After). Portal + AnimatePresence. Sonner toast feedback. 21 tests passing. |
| T313 | process-purchase Edge Function | ✅ | 2026-03-04 | — | Supabase Edge Function for purchase effect processing. 5 effect handlers (extra_notifications, custom_theme, redeem_favor, unlock_feature, generic). Web-push delivery to target user. Notification history insert. Idempotent processing (skips already-processed purchases). CORS preflight handling. |
| T614 | Spiritual Practice Migration | ✅ | 2026-03-04 | a30bafc | Supabase migration 016. 3 tables: prayer_log (5 boolean prayers, unique user_id+date), quran_log (pages_read, daily_goal, unique user_id+date), azkar_sessions (session_type morning/evening CHECK, count, target, unique user_id+date+session_type). Full RLS, indexes, set_updated_at triggers. Types in database.types.ts + spiritual.types.ts. |
| T610 | CouponSendAnimation | ✅ | 2026-03-04 | 547ba8e | Paper airplane fold+flight+12 deterministic particles+"Sent!" overlay. useReducedMotion bypass. Timer cleanup. 16 tests passing. |
| T611 | CouponReceiveAnimation | ✅ | 2026-03-04 | cba1dd4 | Letter descend+bounce overlay with coupon title, Open/Save buttons. Backdrop dismiss. Body scroll lock. useReducedMotion bypass. 20 tests passing. |
| T612 | RedeemStampAnimation Enhancement | ✅ | 2026-03-04 | 8f755ce | Enhanced from basic drop to multi-stage: appear 2x → slam → shake → REDEEMED imprint → 8 ink splatter dots. 17 tests (was 5). |
| T615 | usePrayer + PrayerTracker | ✅ | 2026-03-04 | cb0eab9 | Prayer tracking hook (fetch/toggle/completedCount) + 5-circle component with Arabic+English labels, copper fill, ripple. 26 tests (11 hook + 15 component). |
| T616 | useQuran + QuranTracker | ✅ | 2026-03-04 | 09128b3 | Quran reading hook (logPages/monthlyTotal/dailyGoal) + tracker card with progress bar, increment button. 27 tests (12 hook + 15 component). |
| T617 | useAzkar + AzkarCounter | ✅ | 2026-03-04 | eeafc62 | Azkar counter hook (morning/evening switching, increment/reset, justCompleted) + counter UI with toggle pills, 120px tap area, completion ripple. 34 tests (14 hook + 20 component). |
| T618 | Wire Soul Section | ✅ | 2026-03-04 | eeadb70 | /me/soul page: PrayerTracker → QuranTracker → AzkarCounter with dividers + future placeholder. Replaced EmptyState placeholder. 9 tests passing. |
| T619 | HomePrayerWidget | ✅ | 2026-03-04 | 51a61f3 | Mini prayer dashboard widget on home. 5 mini circles, copper fill, "X/5 prayers today", Link to /me/soul. Returns null when loading/no data. 11 tests passing. |
| T1001 | Shared Lists Migration | ✅ | 2026-03-05 | b770d5f | Supabase migration 017. 2 tables: shared_lists (title, list_type CHECK, created_by FK), list_items (parent_id self-FK for sub-tasks, coyyns_reward, position, completed_by/completed_at, cascade delete). Full RLS with partner access via profiles.partner_id subquery. Indexes on list_id, parent_id, position. Types in database.types.ts. |
| T1002 | useSharedList Hook | ✅ | 2026-03-05 | 591f3eb | Shared list hook with full CRUD (addItem, addSubItem, toggleComplete, deleteItem, reorderItems, createList, deleteList, selectList). Realtime subscription on list_items (INSERT/UPDATE/DELETE). Optimistic updates with rollback. CoYYns reward on completing partner's item. 7-day auto-archive filter on completed items. 19 tests passing (unit/interaction/integration). |
| T1003 | SharedListPage + Components | ✅ | 2026-03-05 | 64c2f8a | QuickAddInput (Enter-to-add, trim, clear), ListItemCard (checkbox, CoYYns badge, sub-items, expand/collapse, delete), SharedListPage (/us/list route with list selector tabs, quick-add, active/completed sections, create new list). Added "List" tab to /us layout. 48 tests passing (13+18+17). |
| T1004 | Rituals Migration | ✅ | 2026-03-05 | 46c30d3 | Supabase migration 018. 2 tables: rituals (title, icon, cadence CHECK daily/weekly/monthly, is_shared, coyyns_reward), ritual_logs (ritual_id FK, period_key UNIQUE per user+ritual, note, photo_url, immutable). RLS: own CRUD on rituals, partner reads shared rituals. Logs: own insert+read, partner reads for shared. Storage bucket ritual-images. Types in database.types.ts + rituals.types.ts. |
| T1005 | useRituals Hook | ✅ | 2026-03-05 | — | Rituals hook with logRitual (period key calc, optimistic insert, CoYYns reward), isLoggedThisPeriod/partnerLoggedThisPeriod (Map-based lookup), createRitual, deleteRitual, uploadRitualPhoto (Supabase Storage). getPeriodKey util (daily/weekly/monthly). Realtime subscription on ritual_logs. 19 tests passing. |
| T1006 | RitualCard + RitualsPage + HomeRitualsWidget | ✅ | 2026-03-05 | cdbe200 | RitualCard (emoji circle, cadence pill, personal/shared status dots, tap-to-log), CreateRitualForm (bottom sheet with emoji picker, cadence selector, shared toggle, CoYYns reward), HomeRitualsWidget (horizontal emoji circles, completion summary, link to /me/rituals), RitualsPage (/me/rituals with cadence grouping, StaggerList, EmptyState, CreateRitualForm modal). Added Rituals section to /me page + HomeRitualsWidget to home. 47 tests passing (12+14+10+11). |
| T1007 | Monthly Letter Ritual | ✅ | 2026-03-05 | — | MonthlyLetterComposer (full-screen writing overlay, serif textarea, "Dear [Partner]" greeting, optional photo upload, send button), LetterCard (envelope aesthetic card, date, preview first line, tap-to-expand overlay with full content), HomeLetterPrompt (1st-of-month CTA on home dashboard). Integrated into RitualsPage: write-letter CTA, letter history section, auto-creates "Monthly Letter" ritual. 34 tests passing (15+14+5). |
| T709 | Event Categories + Color Coding | ✅ | 2026-03-04 | 33dadbc | calendar-constants.ts (4-category config with colors/icons/labels + helpers), EventCategoryBadge (dot+pill variants), calendar.types.ts, useCalendar hook (CRUD + realtime + upcomingEvents/milestones derivations). 42 tests passing (14 hook + 10 constants + 18 badge). |
| T710 | Google Calendar Sync | ✅ | 2026-03-04 | 4d03a52 | Migration 017 (google_calendar_refresh_token + connected_at on profiles). google-calendar.ts (OAuth URL builder + disconnect). OAuth callback API route (code→token exchange). google-calendar-sync Deno edge fn (create/update/delete with token refresh). GoogleCalendarConnect UI (connect/disconnect toggle). 32 tests passing (8 lib + 10 component + 14 Deno). |
| T711 | HomeCountdownWidget | ✅ | 2026-03-05 | — | Milestone countdown widget with Star icon, gold border, days-until counter. "Today!" / singular "day" / plural "days". Uses useCalendar().milestones. Links to /us/calendar?date=. 12 tests passing. |
| T712 | HomeCalendarPeek | ✅ | 2026-03-05 | — | Upcoming events peek (next 3) with category-colored date badges, titles, times. "Coming Up" header + "See All". Empty state with "Add Event" CTA. Both widgets wired into home page after HomePrayerWidget. 17 tests passing. |
| TF02 | Partner Pairing UI | ✅ | 2026-03-04 | 4382d27 | InviteCodeDisplay (code+copy+share), PairPartnerForm (6-char input, RPC pair_partners, confetti success), PairPage (full-screen flow with redirect). Layout integration: unpaired users redirect to /pair. More page: unpair AlertDialog with RPC unpair_partners. database.types.ts updated with unpair_partners function. 32 tests passing (9+15+8). |
| TF03 | Profile Management + Avatar | ✅ | 2026-03-04 | — | Avatar component (4 sizes: sm/md/lg/xl, image+initials fallback, error recovery). uploadAvatar utility (validate+crop+WebP+upload). ProfileEditForm updated to use Avatar+uploadAvatar. More page uses Avatar component. 35 tests passing (14+13+8). |
| TF04 | PWA Infrastructure | ✅ | 2026-03-04 | 46e1cf2 | Serwist service worker (precaching, runtime caching, offline fallback, notification click handler). InstallPrompt (beforeinstallprompt + iOS fallback, 30-day dismiss). notification-router (type-to-route mapping). Offline page. next.config.ts withSerwist wrapper. 24 tests passing (13+11). |
| TF05 | Push Deep Linking | ✅ | 2026-03-04 | 4a7120c | Edge function updated to forward notification.metadata as payload in push data (enables SW type-to-route mapping). SW notificationclick handler routes via getRouteForNotification(). notification-router covers 9 notification types. 24 tests passing (13 Deno + 11 router). |
| TF06 | New Coupon Detection | ✅ | 2026-03-04 | — | useNewCouponDetection hook (localStorage last_seen, mount + visibilitychange recheck, onAnimationComplete/onSaveForLater). CouponReceiveAnimation wired into (main)/layout.tsx as portal overlay. 11 tests passing. |
| TF09 | Purchase Response UI | ✅ | 2026-03-05 | ef58dda | useActivePurchases hook (queries pending/active purchases with marketplace_items join, realtime subscription, acknowledge/complete/decline actions with optimistic updates). ActivePurchaseCard (5 effect types: task_order, veto, wildcard, dnd_timer, extra_ping with inline DNDCountdown SVG ring). Wired into home page above quick actions. 39 tests passing (15 hook + 24 component). |
| TF07 | Media Storage Architecture | ✅ | 2026-03-05 | 24d7ab9 | media_files migration (020) with indexes, RLS, google_drive columns on profiles. media-export edge function (daily cron: Storage→Drive export with folder structure Y2-Media/{table}/{YYYY-MM}/, batch processing, failure isolation). media-proxy edge function (on-demand Drive proxy with 30-day cache headers, API key auth). uploadMedia client utility (validate → WebP compress via OffscreenCanvas → Storage upload → media_files tracking row). Combined Google Drive+Calendar OAuth scope. 39 tests passing (8 export + 10 proxy + 21 upload). |
| TF08 | MediaImage Component | ✅ | 2026-03-05 | — | MediaImage shared component with tier-aware URL resolution (active→Storage, exported→proxy). Shimmer/blur loading placeholders, error state with retry, lazy loading, fallbackUrl backward compat. next.config.ts updated with Supabase image remote patterns. 17 tests passing. |
| T809 | Challenges + Bounties Migration | ✅ | 2026-03-04 | 5862099 | Migration 017_challenges_bounties_v2.sql. ALTER challenges (new statuses, acceptor_id, resolution_note, stakes >= 5). CREATE bounties + bounty_claims tables with RLS. 3 SECURITY DEFINER RPCs (resolve_challenge_payout, refund_challenge_stake, confirm_bounty_claim) for cross-user CoYYns transfers. database.types.ts updated. |
| T810 | useChallenges + useBounties Hooks | ✅ | 2026-03-04 | 2771608 | challenges.types.ts (Challenge, Bounty, BountyClaim + insert/update/status types). useChallenges hook (create/accept/decline/claim/confirm/dispute with spendCoyyns + RPCs). useBounties hook (create/claim/confirm/deny with RPCs). Both with realtime + auth-safe. 29 tests passing (16+13). |
| T811 | Challenge Accept + Resolve Flows | ✅ | 2026-03-04 | 315faa7 | ChallengeAcceptFlow (accept/decline with balance breakdown, insufficient funds warning). ChallengeResolveFlow (4-state: claim/waiting/confirm/disputed with realtime subscription). Portal dialogs. 32 tests passing (16+16). |
| T812 | Challenge Win Animation | ✅ | 2026-03-04 | 6f18c25 | ChallengeWinAnimation. Winner: trophy + 20 confetti particles (copper/gold) + counter 0→amount. Loser: frown + wobble + negative amount. prefers-reduced-motion support (skip particles, 800ms timeout). 16 tests passing. |
| T813 | Standing Bounties UI | ✅ | 2026-03-04 | 3feeb23 | BountyCard (gift icon, reward pill, recurring/pending badges, "I did it!" button). CreateBountyForm (bottom sheet, title+trigger+reward stepper+recurring toggle, RHF+Zod). BountyClaimFlow (creator confirm/deny, claimer waiting). 45 tests passing (13+17+15). |
| T814 | Wire CoYYns Tab | ✅ | 2026-03-04 | 3afaa8b | /us/coyyns page: CoyynsWallet + CoyynsHistory (compact) + Challenges section (pending+active cards, New button) + Bounties section (active cards, New button) + collapsible History. All modals wired: CreateChallengeForm, CreateBountyForm, ChallengeAcceptFlow, ChallengeResolveFlow, ChallengeWinAnimation, BountyClaimFlow. 17 tests passing. |
| T1101 | Vision Board Migration | ✅ | 2026-03-05 | ff3a34f | Supabase migration 022_vision_board.sql: 5 tables (vision_boards, vision_categories, vision_items, monthly_evaluations, category_scores) with RLS, indexes, triggers. Owner CRUD + partner SELECT. Scores CHECK 1-10. Storage bucket vision-board-images. |
| T1102 | useVisionBoard Hook + Types | ✅ | 2026-03-05 | 4b6f5ad | vision-board.types.ts (8 type exports). useVisionBoard hook: full CRUD, board switching, realtime subscription on vision_items, uploadMedia for images, hasEvaluatedThisMonth. 24 tests passing. |
| T1103 | VisionBoardPage + Components | ✅ | 2026-03-05 | 11d02d0 | VisionItemCard (140px, MediaImage/text, achieved badge), CategorySection (horizontal scroll), AddVisionItemForm (bottom sheet), VisionBoardWizard (4-step setup), VisionBoardPage (board switcher, hero banner, eval prompt, wizard empty state). 57 tests passing (16+15+26). |
| T1104 | AddVisionItemForm Tests | ✅ | 2026-03-05 | f5a076a | Full test coverage: title validation, category pre-selection, char counter, save args with trimming, backdrop dismiss, double-submit prevention, category change. 22 tests passing. |
| T1105 | MonthlyEvaluation + EvaluationSlider | ✅ | 2026-03-05 | 7eedbd1 | EvaluationSlider (1-10 range, score labels, optional notes). /2026/evaluate page (month header, per-category sliders, category average, overall score, reflection 1000 chars, already-evaluated guard, submit → router.push). 41 tests passing (21+20). |
| T1106 | EvaluationHistory + ScoreChart | ✅ | 2026-03-05 | 36618de | ScoreChart (custom SVG line chart, 12-month x-axis, 1-10 y-axis, copper self line, gray dashed partner, clickable dots). EvaluationHistory (chart wrapper + month detail breakdown with AnimatePresence). 30 tests passing (15+15). |
| T1107 | VisionBoardWizard Tests | ✅ | 2026-03-05 | cc07958 | Full wizard test coverage: 4-step navigation, category selection/deselection, custom categories, duplicate prevention, Enter key, full onComplete flow, double-click prevention, state preservation. 26 tests passing. |
| T1108 | HomeEvaluationPrompt | ✅ | 2026-03-05 | — | Dashboard card (28th+ of month, board exists, not evaluated, not dismissed). Dismissible 3 days (localStorage). Links to /2026/evaluate. Wired into home page widget stack. 17 tests passing. |
| T1301 | Our Table DB Migration | ✅ | 2026-03-05 | — | Supabase migration 022_our_table.sql: food_visits (14 cols, cuisine_type CHECK 17 values, visit_number auto-increment), food_ratings (9 dimensions 1-10 CHECK, overall_average GENERATED, both_reviewed trigger), food_photos (5 photo types, media_files FK). auto_visit_number() trigger per place. check_both_reviewed() trigger flips when both partners rate. 5 RLS policies on food_visits (own CRUD + partner SELECT), 3 on food_ratings (own insert/select + partner select), 4 on food_photos. food-photos storage bucket (10MB). Realtime enabled on all 3 tables. database.types.ts updated with all 3 table types. Verification SQL in supabase/tests/. |
| T1302 | useFoodJournal Hook | ✅ | 2026-03-05 | — | Food journal data hook: fetches food_visits + food_ratings + food_photos from Supabase. Derived state: stats (totalVisits, uniquePlaces, avgOverall, topCuisine, returnSpots, bookmarkedCount), getVisitById (VisitWithRatings join), getPreferenceDot (vibe masking per dimension), filterByCuisine. Actions: addVisit, updateVisit, toggleBookmark, addRating, addPhotos, removePhoto. Realtime subscription on food_visits. Types in food-journal.types.ts (17 exports: Row/Insert types, CUISINE_TYPES, RATING_DIMENSIONS, PHOTO_TYPES, FoodStats, VisitWithRatings, PreferenceDotColor). Auth-safe: inert state when user null. 17 tests passing. |
| T1501 | Wishlists Migration | ✅ | 2026-03-05 | — | Supabase migration 024_wishlist.sql: wishlists (5 cols, owner_id FK→profiles) + wishlist_items (20 cols, price numeric(10,2), 9 categories CHECK, 3 priorities CHECK, claimed_by SECRET — masked at app layer not RLS). RLS: owner CRUD, partner read + update claim/purchase via get_partner_id(). Auto-create default wishlist trigger on profiles INSERT + backfill. Indexes on wishlist_id, (wishlist_id, is_purchased). |
| T1502 | useWishlist Hook + Types | ✅ | 2026-03-05 | — | wishlist.types.ts (4 types, 2 const arrays with derived types). useWishlist hook: CRITICAL claim masking (myItems always claimed_by: null), dual-list fetching, price totals, CRUD (addItem/removeItem/updateItem), claim/unclaim/markPurchased with own-item guard, realtime subscription, extractUrlMetadata via edge function. url-metadata edge function (Deno, JWT auth, 5s timeout, regex OG extraction). 31 tests passing. |
| T1503 | WishlistPage + Components | ✅ | 2026-03-05 | — | WishlistPage (two tabs, price total, category/priority filters, collapsible purchased, FAB→AddForm, CRITICAL: no claim on own tab). WishlistItemCard (image/emoji, price, category chip, priority pill, own: edit/delete, partner: ClaimBadge). ClaimBadge (3 states: unclaimed/claimed/purchased). AddWishlistItemForm (bottom sheet, URL auto-fill, 9 categories, 3 priorities, validation). Us layout: Wishlist tab added. 57 tests passing (16+18+16+7). |
| T1504 | Enhanced Pairing: QR + Share + Deep Link | ✅ | 2026-03-05 | — | QRCodeDisplay (200px, copper #C4956A on cream #FBF8F4, dynamic qrcode import, Copy Link + Share). QRCodeScanner (getUserMedia, BarcodeDetector primary, jsQR fallback, viewfinder with corner markers). pairing-link.ts (generate/parse URL, sessionStorage bridge). Deep link route /pair/[code] (unauth→store+redirect, auth+unpaired→auto-pair RPC, auth+paired→message). Pair page updated (QR display + scanner + manual entry). Middleware /pair exception. InviteCodeDisplay updated to share link. 50 tests passing (15+12+9+14). |
| T1303 | CreateVisitFlow Step 1: RestaurantSearch | ✅ | 2026-03-05 | 19e5814 | RestaurantSearch component: GPS auto-capture, Nominatim search with 300ms debounce, manual fallback mode, cuisine selection (17 types), date/time auto-fill. Two modes: search (Nominatim API) and manual (free-text place name + address). Result list with AnimatePresence, loading spinner, no-results state. Proper User-Agent header for Nominatim. placeId formatted as nominatim:{osm_type}:{osm_id}. 15 tests passing (search/manual mode toggle, debounce timing, result display+selection, manual submit, no-results, Nominatim URL params, User-Agent header, fetch error, non-ok response, placeId formatting). |
| T1304 | CreateVisitFlow Step 2: FoodPhotoCapture | ✅ | 2026-03-05 | 066580b | FoodPhotoCapture component: 2 required photo slots (food_plate, partner_eating) with file input, preview/remove, extra photos with type selector (5 types excluding required). Back/Next navigation, Next disabled until both required slots filled. createObjectURL for previews, revokeObjectURL on remove. 17 tests passing (required slots render, next disabled/enabled, preview display, extras section, back button, file selection, photo removal with URL revoke, extra type selector, file input attributes, createObjectURL, extra types exclude required). |
| T1305 | CreateVisitFlow Step 3: RatingCarousel | ✅ | 2026-03-05 | 7b71137 | GradientSlider (reusable 1-10 slider with gradient track, snap-to-integer, haptic feedback, label/description, 4 gradient variants: warm/cool/vibrant/muted). VibeCard (special vibe dimension card with animated gradient border, sparkle particles, glow effect). RatingCarousel (9-dimension swipe carousel with dot navigation, 3x3 summary grid, overall average, submit with all 9 scores). 27 tests passing (11 GradientSlider + 16 RatingCarousel). |
| T1306 | RatingReveal + PreferenceDot | ✅ | 2026-03-05 | e6865d4 | PreferenceDot (3-color indicator: match green, close amber, different rose with tooltip on tap, aria-labels). RatingReveal (staggered 8+1 row animation with vibe suspense phase, match messages per dimension, overall score with vibe label, close/replay controls, reduced-motion support). 22 tests passing (10 PreferenceDot + 12 RatingReveal). |
| T1011 | Mood Log Migration + Snap RLS | ✅ | 2026-03-05 | — | Supabase migration 022_mood_log.sql: mood_log table (6 cols, CHECK 6 moods, UNIQUE user_id+mood_date), RLS own+partner access, index, realtime. Snap partner-react UPDATE policy. |
| T1013 | DaysTogetherCounter | ✅ | 2026-03-05 | — | Pure client component: days since paired_at. Two variants (full card, compact). CountUp via Framer Motion animate() with sessionStorage guard. 13 tests passing. |
| T1009 | CameraCapture + useSnap | ✅ | 2026-03-05 | — | snap.types.ts (Snap, SnapSchedule, REACTION_EMOJIS). useSnap hook (today/partner snap, feed, window timer, submitSnap UPDATE/INSERT, reactToSnap, loadMore, realtime). CameraCapture (getUserMedia, facingMode toggle, capture, retake, caption, desktop fallback). /snap/capture page. 26 tests passing. |
| T1012 | MoodPicker + useMood | ✅ | 2026-03-05 | — | mood.types.ts (MOODS, MOOD_EMOJI, MOOD_LABELS). useMood hook (today/partner mood, upsert with optimistic updates, realtime). MoodPicker (6 emoji buttons, pulse animation, note input). PartnerMoodIndicator (partner name+emoji, tappable note card). 40 tests passing. |
| T1010 | SnapFeed + SnapCard + SnapReaction | ✅ | 2026-03-05 | — | /snap feed page (group by date, side-by-side layout, date headers, infinite scroll). SnapCard (MediaImage, avatar overlay, caption scrim, late badge). SnapReaction (5 emoji buttons, select/deselect). 41 tests passing. |
| T1014 | SharedGarden + Migration | ✅ | 2026-03-05 | — | Migration 023_garden.sql: garden_days table (UNIQUE garden_date, yahya/yara_opened booleans, flower_type), RLS all authenticated. useGarden hook (recordOpened upsert, flower bloom on both open, race-safe, 12 flower emojis, recentFlowers derived). SharedGarden component (8-col CSS grid, flowers/seedlings/blanks, staggered animation, compact/full modes). /garden page. 54 tests passing (14 hook + 14 component + 26 widget+page). |
| T1015 | HomeSnapWidget | ✅ | 2026-03-05 | — | Conditional widget: window open → prominent CTA with timer + camera icon → /snap/capture; snapped → thumbnail + checkmark → /snap; nothing → null. 12 tests passing. |
| T1016 | Wire Dashboard | ✅ | 2026-03-05 | — | Home page rewired to 15+ sections: Greeting+MoodPicker, PartnerMoodIndicator, MoodStrip, HomeSnapWidget, CoyynsWidget, HomeMarketplaceRow, ActivePurchases, HomeCouponInbox, FeelingGenerousCTA, HomeRitualsWidget, HomeCalendarPeek, HomeCycleWidget, HomePrayerWidget, SharedGarden (compact), DaysTogetherCounter, LetterPrompt, EvaluationPrompt, CountdownWidget. QuickActionCards removed. recordOpened() called on mount. 20 tests passing. |
| T1307 | Our Table Map Dashboard | ✅ | 2026-03-05 | 23318d4 | FoodMap (Leaflet + OSM tiles, warm CSS filter, CircleMarker pins, FitBounds), MapPinCard (AnimatePresence bottom sheet on pin tap, dismiss on backdrop/close), VisitListItem (horizontal card with MapPin icon, cuisine pill, score badge), OurTablePage (map/list toggle, 3 filter types: cuisine/high-score/return-spots, FAB, empty state). 23 tests passing. |
| T1308 | Visit Detail Page | ✅ | 2026-03-05 | 1353e73 | VisitDetailPage (photo gallery, rating bars per dimension, overall score badge, notes auto-save, return history with score trends, mini-map, share card generation, rate CTA, replay reveal overlay). MiniMap (small Leaflet map, dragging disabled, warm filter). 24 tests passing. |
| T1601 | Onboarding State Machine | ✅ | 2026-03-05 | — | Migration 027_onboarding.sql (onboarding_step + onboarding_completed_at on profiles, existing paired users auto-complete). onboarding.types.ts (9-step union, TOUR_STEPS subset). use-onboarding.ts hook (goNext/goBack/complete/skip, DB persistence, direction tracking). (main)/layout.tsx guard (redirect to /onboarding if incomplete, suppress ProfileSetupOverlay + pairing redirect). database.types.ts updated. 22 tests passing. |
| T1602 | Onboarding Shell Layout | ✅ | 2026-03-05 | — | /onboarding layout (fixed z-40), page.tsx (step router), OnboardingShell (animated gradient bg + copper progress bar + skip), StepTransition (direction-aware AnimatePresence). BottomNav hidden during onboarding. globals.css @keyframes onboarding-gradient. |
| T1603 | Welcome Screen | ✅ | 2026-03-05 | — | WelcomeStep: staggered 4.5s animation (Arabic حَيَاة → Hayah → tagline → philosophy → Begin button). Amiri font added to root layout. useReducedMotion support. Double-click prevention. 11 tests passing. |
| T1604 | Profile Setup Step | ✅ | 2026-03-05 | — | ProfileStep: progressive reveal (greeting → name input → avatar → continue). Auto-focus, copper glow, avatar upload to Supabase Storage. Continue disabled until name ≥ 2 chars. 16 tests passing. |
| T1605 | Partner Pairing Step | ✅ | 2026-03-05 | — | PairingStep: unpaired state (reuses InviteCodeDisplay, QRCodeDisplay, PairPartnerForm) + celebration state. Realtime subscription on profiles for partner detection. PairingCelebration: names + heart + confetti + 3s delayed button + double-click guard. 24 tests passing (13 PairingStep + 11 PairingCelebration). |
| T1606 | SpotlightOverlay System | ✅ | 2026-03-05 | — | SpotlightOverlay: SVG mask spotlight hole (rect/circle/pill), pulse ring, tooltip card with step dots/navigation. use-spotlight hook: DOM querySelector targeting, ResizeObserver, scroll lock, debounced resize. 27 tests passing (14 overlay + 13 hook). |
| T1607 | TourHomeStep + Foundation | ✅ | 2026-03-05 | — | BottomNav: exported NAV_TABS + data-testid. SpotlightOverlay: lastButtonText/lastButtonClassName props. BottomNavPreview: display-only nav for tour (inert, pointer-events-none, aria-hidden). TourStep: reusable tour step component (auto-start spotlight, config-driven). TourHomeStep: Home tab config wrapper. 23 tests passing (9 BottomNavPreview + 7 TourStep + 7 TourHomeStep). |
| T1608 | TourUsStep | ✅ | 2026-03-05 | — | Us tab tour config: circle shape, "The fun zone. CoYYns, coupons, marketplace…", step 2/5. 6 tests passing. |
| T1609 | Tour2026Step | ✅ | 2026-03-05 | — | 2026 tab tour config: pill shape (wider for elevated center tab), "Your shared vision board…", step 3/5. 6 tests passing. |
| T1610 | TourMeStep | ✅ | 2026-03-05 | — | Me tab tour config: circle shape, "Your personal space…", step 4/5. 5 tests passing. |
| T1611 | TourMoreStep | ✅ | 2026-03-05 | — | More tab tour config: circle, pulseTarget=false, "Settings, preferences…", step 5/5, isLast=true. "Finish Tour →" button with copper glow. 8 tests passing. |
| T1612 | ReadyStep | ✅ | 2026-03-05 | — | Personalized launch screen: user + partner avatars (lg, copper ring), "Welcome home, {name}", paired/solo subtitle, mini confetti (15 particles, copper, paired only), "Let's begin →" after 1s, exit animation (scale+fade 300ms), double-click prevention. 17 tests passing. |
| T1613 | Integration Hardening | ✅ | 2026-03-05 | — | Onboarding page: wired all 5 tour steps + ReadyStep, error banner with retry. Layout: deep link ?code= forwarding to /onboarding, CouponReceiveAnimation suppressed during onboarding. PairingStep: initialCode prop → PairPartnerForm auto-fill. 165 total tests passing across 15 files. |
| — | Settings/Profile Page Full Implementation | ✅ | 2026-03-07 | — | Fully functional settings page: (1) Notifications sub-page `/more/notifications` with push toggle, permission banners, device list + removal. (2) Google Drive connect/disconnect UI mirroring Calendar pattern. (3) Data & Storage section with storage estimate display, cache clearing. (4) Profile polish — role badge, dynamic version, language placeholder. 10 new files, 34 new tests all passing. Build clean. |
