# T110: /health Health Module Shell

Build the health module placeholder page.

## Read First
- `CLAUDE.md`
- `docs/DESIGN_SYSTEM.md`
- `docs/COMPONENT_REGISTRY.md`

## What to Build

File: `src/app/health/page.tsx`
Test: `src/__tests__/app/health/page.test.tsx`

## Page Structure

Simple placeholder page. Content will be added in Phase 5 (period tracker) and beyond.

```tsx
import { PageHeader } from "@/components/shared/PageHeader"
import { PageTransition } from "@/components/animations"
import { EmptyState } from "@/components/shared/EmptyState"
import { Activity } from "lucide-react"

export default function HealthPage() {
  return (
    <PageTransition>
      <PageHeader title="Health" backHref="/" />
      <div className="px-6 py-6">
        <EmptyState
          icon={<Activity size={48} />}
          title="Health tracking coming soon"
          subtitle="Fitness, wellness, and more — all in one place"
        />
      </div>
    </PageTransition>
  )
}
```

## Test Requirements

- Page renders without error
- PageHeader shows "Health"
- EmptyState is visible

## Do NOT

- Don't add tabs or sections — this is a flat placeholder
- Don't add any data fetching
